#include "main.h"

/**
 * _islower - checks for lowercase character.
 * @c: Variable to check for.
 *
 * Return:  1 if c i lowercase character, fail (0)
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
